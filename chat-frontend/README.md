# WeatherGPT Chat Frontend

Standalone React/Vite frontend containing only the WeatherGPT chat UI.

## Run

```bash
npm install
npm run dev
```

Backend/API integration is intentionally left for the backend developer.
Set `VITE_API_URL` in `.env` when the API contract is ready.
