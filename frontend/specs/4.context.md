## location button
- inside the onClick handler fetch the latitude and longitute of the user of there current location

## location hook
- create a hook which gives a weather report in json format
- it comes from api/v1/weather
- fetch the weatherJSON content in the place of hardcoded data
- send it directly to weatherJSON in dashboard
- the api/v1/weather expects a lat and long parameter
- the lat and long will come from location button which is in dashboard

## overview
- before the location btn is clicked the by deafult location from the location btn should be of new delhi