## animation
- install motion
- use it for all the current and upcoming animations
- fine tune every animation 

## pages/chat
- persist the inputBox and send button on screen
- the chat section should be scroll-able only betweeen navbar and inputBox
- with margin at the top and bottom of chat section also five a little fade 
- re-align the text inside the chatBox
    - left align for ai reponse
    - right align for user
- while the ai response is in promise a loader should come in chatBox
    - three dot jumping one after another after a delay
- stream the ai response
    - word by word it should some
    - a fade in blur animation for word
- persist the chats until the page is reloaded explicitly not while changing the pages


